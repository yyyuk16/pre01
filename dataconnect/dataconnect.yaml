specVersion: "v1"
serviceId: "pre"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pre-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
